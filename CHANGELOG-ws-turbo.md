# بازنویسی لایه‌ی WebSocket — نسخه‌ی Turbo

پروتکل VLESS دست‌نخورده است (کلاینت‌های Xray بدون هیچ تغییری کار می‌کنند)؛ فقط لایه‌ی انتقال داده در `relay_vless.py` بازنویسی شد.

## تاخیر (مهم‌ترین عامل حس سرعت)

- **Early Data** (`?ed=2048` در لینک): اولین پکت داخل هدر `Sec-WebSocket-Protocol` همراه خود دست‌دادن می‌آید → **یک RTT کامل** از باز شدن هر اتصال حذف می‌شود (در مرورگری که ده‌ها اتصال می‌زند خیلی محسوس است).
- **اتصال موازی به مقصد (Happy-Eyeballs)**: تا ۳ اتصال همزمان روی IPv4/IPv6 مختلف باز می‌شود، سریع‌ترین برنده و بقیه بسته می‌شوند (فاصله‌ی شروع ۱۲۰ms).
- **کش DNS با TTL ۵ دقیقه**: تاخیر resolve از اتصال‌های بعدی حذف می‌شود.
- پکت اول بدون `drain` اجباری راهی می‌شود و هدر پاسخ VLESS در فریم جداگانه می‌رود → حذف کپی کل اولین چانک.

## توان عبوری

- بافر خواندن **تطبیقی**: ۲۵۶KB ↔ ۴MB (پر شد → دوبرابر، خلوت شد → نصف).
- `high-water` نوشتن **AIMD**: ۲۵۶KB تا ۳۲MB بر اساس زمان واقعی `drain`.
- سوکت: `TCP_NODELAY` + `SO_SNDBUF`/`SO_RCVBUF` = **۸MB** + `TCP_QUICKACK` + سقف بافر ترانسپورت (هم سمت مقصد، هم تا جایی که سرور اجازه دهد سمت کلاینت).
- **مسیر سریع**: اگر کانفیگ لیمیت سرعت ندارد، تراتل کاملاً دور زده می‌شود (هر ۶۴ فریم وضعیت دوباره چک می‌شود).
- حسابرسی حجم دسته‌ای با `QuotaGate` (بدون قفل سراسری به‌ازای هر چانک) و `flush()` در پایان.
- سرور: `uvloop` + `httptools` (اگر نصب باشند)، `ws_max_size=32MB`، ping خودکار خاموش، `backlog=4096`.

## موازی‌سازی اتصال‌ها

- در WebSocket هر جریان TCP یک اتصال جداگانه دارد و تعداد آن را کلاینت تعیین می‌کند؛ سرور نمی‌تواند یک جریان را روی چند WS پخش کند مگر اینکه کلاینت هم همان پروتکل را بفهمد. بنابراین:
  - سمت سرور: اتصال موازی در فاز connect + محدودیت صفر روی تعداد اتصال‌های هم‌زمان.
  - برای موازی‌سازی واقعی روی یک جریان، لینک‌های XHTTP حالا پارامتر `extra` با **xmux** دارند: `maxConnections 4-8` و `maxConcurrency 16-32` → کلاینت چندین اتصال HTTP/2 موازی باز می‌کند.

## دقت

- هدر VLESS می‌تواند تکه‌تکه برسد → تجمیع ایمن تا سقف ۱۶KB و پارس مقاوم در برابر داده‌ی ناقص (قبلاً فرض می‌شد کل هدر در یک فریم است).
- حجم دقیقاً یک‌بار شمرده می‌شود و دسترسی به رکورد اتصال پس از بسته شدن خطا نمی‌دهد.

## نتیجه‌ی تست خودکار (لوپ‌بک)

```
single-frame header: resp-header=True match=True
split header      : resp-header=True match=True
early-data (ed)   : resp-header=True match=True
parallel connect  : first=0.9ms  cached=0.4ms
throughput        : 64MB round-trip in 0.20s ≈ 315 MB/s هر جهت
```
