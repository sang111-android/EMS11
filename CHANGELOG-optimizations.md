# تغییرات این نسخه — دقت بیشتر، سرعت بیشتر، دو مد جدید

## مدهای جدید

| مد | مسیر | توضیح |
|---|---|---|
| `xhttp-packet-up` | `POST .../{sid}/{seq}` + `GET .../{sid}` | سازگارترین با CDN |
| `xhttp-stream-up` | `POST .../{sid}` + `GET .../{sid}` | آپلینک پیوسته |
| **`xhttp-stream-one`** | فقط یک `POST .../{sid}` دوطرفه | کمترین تاخیر؛ نیازمند HTTP/2 (ALPN = `h2`) |
| **`xhttp-auto`** | هر دو را می‌پذیرد | روی H2 → stream-one، روی HTTP/1.1 → packet-up |

هر دو مد در پنل وب، لینک‌ساز، ساب‌لینک و ربات تلگرام در دسترس‌اند.

## سرعت

- **`QuotaGate` مشترک** (`speed_limit.py`): حسابرسی حجم به‌صورت دسته‌ای و تطبیقی (۳۲KB تا ۱MB، EWMA روی نرخ) به‌جای گرفتن قفل سراسری `LINKS` به‌ازای هر چانک. حالا در هر چهار مد XHTTP **و** در مسیر WebSocket استفاده می‌شود.
- **`_AdaptiveFlow` (AIMD)**: سقف بافر نوشتن بر اساس زمان `drain` کم/زیاد می‌شود (۲۵۶KB–۱۶MB). قبلاً فقط در stream-up بود؛ حالا packet-up هم از آن استفاده می‌کند (به‌جای آستانه‌ی ثابت ۲MB).
- **تیون سوکت**: `TCP_NODELAY` + `SO_SNDBUF`/`SO_RCVBUF` = ۴MB در هر دو مسیر XHTTP و WebSocket.
- **تراتل سبک‌تر**: `MIN_BURST` از ۱۶KB به ۶۴KB و سقف خواب از ۰.۵ث به ۰.۲۵ث → وقفه‌های کمتر و کوتاه‌تر.
- **پدینگ تصادفی `x-padding`** در پاسخ‌های XHTTP کنار `x-accel-buffering: no` — مانع بافر کردن پراکسی‌های میانی و شکستن الگوی طول پاسخ.

## دقت و رفع اشکال

- **رفع حلقه‌ی بی‌پایان در تراتل**: اگر چانک از ظرفیت باکت بزرگ‌تر بود (مثلاً چانک ۵۱۲KB روی لیمیت ۱Mbps) مصرف هرگز تکمیل نمی‌شد و اتصال قفل می‌کرد؛ حالا چانک خودکار به برش‌های هم‌اندازه‌ی ظرفیت تقسیم می‌شود.
- **دقت محدودیت سرعت**: `Mbps` حالا دهدهی حساب می‌شود (۱۰^۶ بیت)، نه ۲^۲۰ — قبلاً حدود ۵٪ بیشتر از مقدار تنظیم‌شده می‌داد.
- **رفع نشتی سشن/حافظه**: ریپر دوسطحی — سشن بی‌تونل پس از ۳۰ ثانیه، سشن فعال پس از ۳۰ دقیقه بیکاری جمع می‌شود (قبلاً سشن با تونل باز هرگز جمع نمی‌شد). همچنین `reset_bucket` هنگام حذف کانفیگ.
- **جلوگیری از ددلاک**: `_teardown` دیگر منتظر تسکی که خودش را صدا زده نمی‌ماند؛ سشن اول از جدول حذف می‌شود پس فراخوانی دوباره بی‌اثر است.
- **حفاظت ورودی**: طول `session_id` حداکثر ۶۴ کاراکتر، سقف ۵۰۰۰ سشن هم‌زمان، سقف ۱۲۸ پکت خارج از ترتیب در packet-up، و رد کردن استفاده‌ی مجدد یک `session_id` توسط UUID دیگر.
- **آمار دقیق‌تر**: `total_requests` به‌ازای هر درخواست HTTP شمرده می‌شود، نه هر چانک.
- **رفع `KeyError`**: دسترسی به `connections[conn_id]` پس از بسته شدن سشن دیگر استثنا نمی‌دهد.
- **کنترل مسیر**: هر مد فقط مسیرهای مجاز خودش را می‌پذیرد (مثلاً stream-one دانلینک GET جدا ندارد) و باز کردن تونل TCP زیر قفل است تا دو درخواست هم‌زمان دو تونل موازی نسازند.
- حسابرسی نهایی (`flush`) در پایان هر مسیر، تا حجم مصرفی از دست نرود.
